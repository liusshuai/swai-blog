import { SpinIcon } from '@swai/icon';

export default () => {
    return (
        <div className="h-screen flex items-center justify-center">
            <SpinIcon className="animate-spin" size={36} />
        </div>
    );
};
